{
  "$schema": "https://opencode.ai/config.json",
  "tools": {
    "firefight": {
      "description": "Debate + exploit vulnerability (6 rounds, 5 agents). Accepts --target <url> --finding <description>",
      "command": "node",
      "args": ["scripts/firefight.js", "--target", "$TARGET", "--finding", "$FINDING"]
    }
  }
}
